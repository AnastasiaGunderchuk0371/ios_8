// Создаем структуру
struct Point {
    var x: Int
    var y: Int
}

// Создаем класс
class Player {
    var name: String
    var score: Int
    
    init(name: String, score: Int) {
        self.name = name
        self.score = score
    }
    
    func increaseScore(by points: Int) {
        self.score += points
    }
}

//В данном примере, структура Point представляет собой тип значения, который содержит два свойства x и y. 
//Структуры обладают семантикой "значение", то есть они копируются при передаче по значению (при передаче аргументов в функции или методы).

//Класс Player представляет собой тип ссылки, который содержит свойства и методы. 
//Классы обладают семантикой "ссылки", то есть при передаче экземпляра класса они передаются по ссылке, и изменения в одном экземпляре отразятся на всех ссылках на этот экземпляр.
